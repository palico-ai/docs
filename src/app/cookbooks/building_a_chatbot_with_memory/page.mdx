# Building a Chatbot with Memory
In this guide, we'll build an LLM application that can keep memory across multiple messages. We'll use the `ChatHistoryStore` class provided by Palico to store and retrieve conversation states.

<Note>
This guide is a work in progress.
</Note>